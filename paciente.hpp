#include <iostream>
#pragma once

/*Clase que trabaja con los datos de la persona o paciente*/
class paciente{

private:

  std::string nombre;
  int codigo;
  int edad;
public:

  paciente(std::string name,int cd, int age){
    
  }
};
  
